Replace game.js and style.css in the root of your GitHub repository.
The 10 noun illustrations are now embedded directly inside game.js, so they no longer depend on assets/images paths.
After committing, wait for Pages to deploy and hard-refresh with Ctrl+F5.
